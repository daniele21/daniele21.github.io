---
/** Plane 02: Architecture & Infrastructure / BUILD. The graphic is managed independently. */
import { landingData } from '../../content/locales/en/landing';
import PlaneTemplate from './PlaneTemplate.astro';
import EcosystemDiagram from './EcosystemDiagram.astro';

const { buildStage } = landingData;
---

<PlaneTemplate
  id="infrastructure"
  number={2}
  side="right"
  surface="subtle"
  kicker=""
  title={buildStage.title}
  intro={buildStage.intro}
  handoff={buildStage.handoff}
  nextLabel="TEST"
  visualLabel="Local AI Execution Layer Ecosystem Diagram"
>
  <EcosystemDiagram />
</PlaneTemplate>
