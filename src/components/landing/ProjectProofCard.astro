---
/**
 * ProjectProofCard.astro
 * Semantic proof card with tailored anatomies for Proof (Applications) and Evidence.
 * Strictly adheres to 14px minimum meaningful font size and aligns with the Build layer style.
 */
import type { ProjectCardFeature } from '../../types/landing';
import PlaneCard from './PlaneCard.astro';

interface Props {
  plane?: string;
  variant?: 'execution' | 'proof' | 'evidence' | 'compact';
  tag?: string;
  eyebrow?: string;
  title: string;
  description: string;
  href: string;
  logoPath?: string;
  logoAlt?: string;
  imagePath?: string;
  imageAlt?: string;
  tone?: 'red' | 'purple' | 'blue' | 'green';
  symbol?: 'audio' | 'performance' | 'boundary' | 'feedback';
  features?: ProjectCardFeature[];
  evidenceLabel?: string;
  evidence?: string;
  ctaLabel: string;
  whatItTests?: string;
}

const {
  variant = 'proof',
  tag,
  eyebrow,
  title,
  description,
  href,
  logoPath,
  logoAlt = '',
  imagePath,
  imageAlt = '',
  tone = 'blue',
  symbol,
  features,
  evidenceLabel,
  evidence,
  ctaLabel,
  whatItTests,
} = Astro.props;

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
const asset = (path: string) => `${baseUrl}/${path.replace(/^\//, '')}`;
const isMobile = Boolean(
  (tag && tag.toLowerCase().includes('mobile')) ||
  title.toLowerCase().includes('aura') ||
  (imagePath && imagePath.includes('aura'))
);
---

<PlaneCard
  href={href}
  accent={tone === 'purple' ? 'violet' : 'blue'}
  className={`project-proof-card project-proof-card--${variant}${tone ? ` project-proof-card--tone-${tone}` : ''}`}
  ariaLabel={`${title}: ${description}`}
>
  {variant === 'compact' ? (
    <div class="proof-vertical-card">
      <!-- Top: Identity & Header -->
      <div class="proof-vertical-card__header">
        <div class="proof-vertical-card__topline">
          <div class="proof-vertical-card__icon-box" aria-hidden={!logoPath ? 'true' : undefined}>
            {logoPath ? (
              <img src={asset(logoPath)} alt={logoAlt} loading="lazy" />
            ) : (
              <svg class="proof-vertical-card__symbol" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            )}
          </div>
          <div class="proof-vertical-card__header-meta">
            <span class="proof-vertical-card__tag">{tag || eyebrow || 'REFERENCE APPLICATION'}</span>
            <h3 class="proof-vertical-card__title">{title}</h3>
          </div>
        </div>

        <p class="proof-vertical-card__question">{description}</p>
      </div>

      <!-- Center: Product Interface Showcase -->
      {imagePath && (
        <div class="proof-vertical-card__preview">
          <div class="proof-vertical-card__screen">
            <img class="proof-vertical-card__image" src={asset(imagePath)} alt={imageAlt} loading="lazy" />
            <span class="proof-vertical-card__screen-glare" aria-hidden="true"></span>
          </div>
        </div>
      )}

      <!-- Bottom Body: Validation Section with Chips -->
      {features && features.length > 0 && (
        <div class="proof-vertical-card__validation">
          <span class="proof-vertical-card__validation-label">WHAT THIS VALIDATES:</span>
          <div class="proof-vertical-card__validation-chips">
            {features.map((feature) => (
              <span class="validation-chip">{feature.label}</span>
            ))}
          </div>
        </div>
      )}

      <!-- Bottom: Footer CTA -->
      <div class="proof-vertical-card__footer">
        <strong class="proof-vertical-card__cta-text">{ctaLabel}</strong>
        <span class="cta-arrow" aria-hidden="true">→</span>
      </div>
    </div>
  ) : (
    <Fragment slot="header">
      {eyebrow && (
        <div class="project-proof-card__meta">
          <span class="project-proof-card__eyebrow">{eyebrow}</span>
        </div>
      )}
      <span class="project-proof-card__arrow" aria-hidden="true">↗</span>
    </Fragment>

    <div class="project-proof-card__content">
      <div class="project-proof-card__identity">
        <div class="project-proof-card__logo" aria-hidden={!logoPath ? 'true' : undefined}>
          {logoPath ? (
            <img src={asset(logoPath)} alt={logoAlt} loading="lazy" />
          ) : (
            <svg class="project-proof-card__symbol" viewBox="0 0 48 48" aria-hidden="true">
              {symbol === 'audio' && <path d="M8 27h5l3-10 5 20 5-27 5 23 4-12 3 6h4" />}
              {symbol === 'performance' && <path d="M8 36V10m0 26h32M12 31l8-8 7 4 11-14" />}
              {symbol === 'boundary' && <><path d="M24 6 38 12v10c0 9-5 15-14 20-9-5-14-11-14-20V12z" /><path d="M17 24h14M24 17v14" /></>}
              {symbol === 'feedback' && <><path d="M10 19a16 16 0 0 1 27-5l3 4" /><path d="m40 10v8h-8M38 29a16 16 0 0 1-27 5l-3-4" /><path d="M8 38v-8h8" /></>}
            </svg>
          )}
        </div>

        <div class="project-proof-card__titles">
          <h3 class="project-proof-card__title">{title}</h3>
          <p class="project-proof-card__question">{description}</p>
        </div>
      </div>

      {features && features.length > 0 && (
        <div class="project-proof-card__tags">
          {features.map((f) => (
            <span class="proof-tag">{f.label}</span>
          ))}
        </div>
      )}

      {whatItTests && (
        <div class="project-proof-card__body-block">
          <span class="project-proof-card__badge-label">
            {whatItTests ? 'WHAT IT TESTS' : evidenceLabel}
          </span>
          <p class="project-proof-card__body-text">
            {whatItTests || evidence}
          </p>
        </div>
      )}
    </div>

    <Fragment slot="footer">
      <div class="project-proof-card__footer-inner">
        <strong class="project-proof-card__cta">{ctaLabel}<span aria-hidden="true"> →</span></strong>
      </div>
    </Fragment>
  )}
</PlaneCard>

<style>
  /* Base Proof Card Styles (Standard Variant) */
  .project-proof-card__meta {
    min-width: 0;
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--plane-card-accent);
    font-size: var(--text-badge);
    font-weight: 850;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .project-proof-card__arrow {
    color: var(--ink-muted);
    font-size: 1rem;
    transition: transform var(--duration-fast) ease;
  }

  :global(.project-proof-card:hover) .project-proof-card__arrow,
  :global(.project-proof-card:focus-visible) .project-proof-card__arrow {
    transform: translate(2px, -2px);
    color: var(--plane-card-accent);
  }

  .project-proof-card__identity {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    padding-bottom: var(--space-3);
  }

  .project-proof-card__logo {
    width: 48px;
    height: 48px;
    display: grid;
    place-items: center;
    border: 1px solid var(--border-medium);
    border-radius: var(--radius-sm);
    background: var(--surface-subtle);
    flex-shrink: 0;
  }

  .project-proof-card__logo img {
    width: 32px;
    height: 32px;
    display: block;
    object-fit: contain;
  }

  .project-proof-card__symbol {
    width: 28px;
    height: 28px;
    fill: none;
    stroke: var(--plane-card-accent);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .project-proof-card__titles {
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .project-proof-card__title {
    margin: 0 0 3px;
    color: var(--ink-display);
    font-family: var(--font-display);
    font-size: 1.08rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .project-proof-card__question {
    margin: 0;
    color: var(--ink);
    font-size: var(--text-caption);
    font-weight: 600;
    line-height: 1.4;
    text-wrap: pretty;
  }

  .project-proof-card__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-top: auto;
    padding-top: var(--space-2);
  }

  .proof-tag {
    font-size: var(--text-badge);
    font-weight: 650;
    color: var(--ink);
    background: var(--surface-subtle);
    border: 1px solid var(--border-subtle);
    padding: 2px 8px;
    border-radius: var(--radius-sm);
  }

  .project-proof-card__body-block {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: var(--space-3);
    background: var(--surface-subtle);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-sm);
    margin-top: auto;
  }

  .project-proof-card__badge-label {
    font-family: var(--font-display);
    font-size: var(--text-badge);
    font-weight: 850;
    letter-spacing: 0.08em;
    color: var(--plane-card-accent);
    text-transform: uppercase;
  }

  .project-proof-card__body-text {
    margin: 0;
    color: var(--ink-muted);
    font-size: var(--text-caption);
    line-height: 1.45;
  }

  .project-proof-card__footer-inner {
    width: 100%;
    display: flex;
    justify-content: flex-end;
  }

  .project-proof-card__cta {
    min-height: 38px;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    color: var(--plane-card-accent);
    font-family: var(--font-display);
    font-size: var(--text-caption);
    font-weight: 750;
    white-space: nowrap;
  }

  /* ==========================================================================
     VERTICAL COMPACT CARD (PLANE 03 - TEST APPLICATIONS)
     Cohesive with Plane 02 BUILD Layer Aesthetics & Dimensions
     ========================================================================== */

  :global(.plane-card.project-proof-card--compact) {
    --product-accent: #2563eb;
    --product-accent-light: #38bdf8;
    --product-soft: rgba(37, 99, 235, 0.08);
    --product-border: rgba(56, 189, 248, 0.28);
    --product-glow: rgba(56, 189, 248, 0.35);

    height: 100%;
    min-height: 440px;
    display: flex;
    flex-direction: column;
    padding: 0;
    border-radius: 18px;
    border: 1px solid color-mix(in srgb, var(--product-accent) 22%, var(--border-medium));
    background:
      radial-gradient(circle at 50% 0%, var(--product-soft), transparent 55%),
      var(--surface-card);
    box-shadow:
      0 4px 20px rgba(8, 19, 51, 0.04),
      inset 0 1px 0 rgba(255, 255, 255, 0.95);
    transition:
      transform var(--duration-base) var(--ease-out-expo),
      border-color var(--duration-fast) ease,
      box-shadow var(--duration-base) ease;
  }

  /* Tone 1: Blue / Azure (RedactGuard) */
  :global(.plane-card.project-proof-card--tone-blue),
  :global(.plane-card.project-proof-card--tone-red) {
    --product-accent: #2563eb;
    --product-accent-light: #38bdf8;
    --product-soft: rgba(37, 99, 235, 0.08);
    --product-border: rgba(56, 189, 248, 0.28);
    --product-glow: rgba(56, 189, 248, 0.35);
  }

  /* Tone 2: Emerald Green (Aura Finance) */
  :global(.plane-card.project-proof-card--tone-green) {
    --product-accent: #059669;
    --product-accent-light: #34d399;
    --product-soft: rgba(16, 185, 129, 0.08);
    --product-border: rgba(52, 211, 153, 0.28);
    --product-glow: rgba(52, 211, 153, 0.35);
  }

  /* Tone 3: Violet / Purple (ClosedRoom) */
  :global(.plane-card.project-proof-card--tone-purple) {
    --product-accent: #7c3aed;
    --product-accent-light: #a855f7;
    --product-soft: rgba(124, 58, 237, 0.08);
    --product-border: rgba(168, 85, 247, 0.28);
    --product-glow: rgba(168, 85, 247, 0.35);
  }

  :global(.project-proof-card--compact .plane-card__header) {
    display: none;
  }

  :global(.project-proof-card--compact .plane-card__footer) {
    display: none;
  }

  :global(.project-proof-card--compact .plane-card__body) {
    display: flex;
    flex-direction: column;
    flex: 1;
    padding: 0;
    overflow: visible;
  }

  :global(.plane-card.project-proof-card--compact:hover),
  :global(.plane-card.project-proof-card--compact:focus-visible) {
    transform: translateY(-4px);
    border-color: color-mix(in srgb, var(--product-accent) 48%, var(--border-medium));
    box-shadow:
      0 16px 36px rgba(8, 19, 51, 0.09),
      0 0 28px var(--product-soft),
      inset 0 1px 0 rgba(255, 255, 255, 0.98);
  }

  /* Vertical Card Layout */
  .proof-vertical-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    min-width: 0;
  }

  .proof-vertical-card__header {
    padding: 20px 20px 14px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  /* Topline */
  .proof-vertical-card__topline {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .proof-vertical-card__icon-box {
    width: 44px;
    height: 44px;
    display: grid;
    place-items: center;
    border-radius: 12px;
    flex-shrink: 0;
    background: linear-gradient(145deg, var(--product-soft), color-mix(in srgb, var(--product-accent) 14%, transparent));
    border: 1px solid var(--product-border);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.6),
      0 3px 8px rgba(8, 19, 51, 0.04);
    transition: transform var(--duration-fast) ease, box-shadow var(--duration-fast) ease;
  }

  .proof-vertical-card__icon-box img {
    width: 26px;
    height: 26px;
    object-fit: contain;
    display: block;
  }

  .proof-vertical-card__symbol {
    width: 22px;
    height: 22px;
    color: var(--product-accent);
  }

  .proof-vertical-card__header-meta {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }

  .proof-vertical-card__tag {
    color: var(--product-accent);
    font-family: var(--font-display);
    font-size: 0.64rem;
    font-weight: 850;
    letter-spacing: 0.08em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .proof-vertical-card__title {
    margin: 0;
    color: var(--ink-display);
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.2;
    text-wrap: balance;
  }

  .proof-vertical-card__question {
    margin: 0;
    color: var(--ink-muted);
    font-size: 0.875rem;
    font-weight: 560;
    line-height: 1.4;
    text-wrap: pretty;
  }

  /* Product Preview Screen (Frameless clean stage) */
  .proof-vertical-card__preview {
    padding: 0 18px 16px;
    display: flex;
    flex-direction: column;
  }

  .proof-vertical-card__screen {
    width: 100%;
    height: 196px;
    position: relative;
    overflow: hidden;
    border-radius: 12px;
    border: 1px solid color-mix(in srgb, var(--product-accent) 22%, var(--border-medium));
    background: #081333;
    box-shadow:
      0 6px 18px rgba(8, 19, 51, 0.10),
      inset 0 1px 0 rgba(255, 255, 255, 0.12);
    display: flex;
  }

  .proof-vertical-card__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
    transition:
      transform var(--duration-base) var(--ease-out-expo),
      filter var(--duration-base) ease;
    filter: saturate(0.98) contrast(1.04);
  }

  .proof-vertical-card__screen-glare {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(180deg, rgba(8, 19, 51, 0.04), transparent 40%, rgba(8, 19, 51, 0.22));
  }

  /* Validation Block */
  .proof-vertical-card__validation {
    padding: 0 20px 16px;
    display: flex;
    flex-direction: column;
    gap: 7px;
  }

  .proof-vertical-card__validation-label {
    color: var(--product-accent);
    font-family: var(--font-display);
    font-size: 0.62rem;
    font-weight: 850;
    letter-spacing: 0.08em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .proof-vertical-card__validation-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .validation-chip {
    display: inline-flex;
    align-items: center;
    padding: 3px 8px;
    font-size: 0.72rem;
    font-weight: 650;
    line-height: 1.2;
    color: var(--ink);
    background: var(--surface-subtle);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-sm);
    transition:
      border-color var(--duration-fast) ease,
      background-color var(--duration-fast) ease;
  }

  :global(.plane-card.project-proof-card--compact:hover) .validation-chip {
    border-color: color-mix(in srgb, var(--product-accent) 24%, var(--border-subtle));
  }

  /* CTA Footer */
  .proof-vertical-card__footer {
    margin-top: auto;
    padding: 14px 20px;
    border-top: 1px solid color-mix(in srgb, var(--product-accent) 12%, var(--border-subtle));
    background: linear-gradient(90deg, var(--product-soft), transparent);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .proof-vertical-card__cta-text {
    font-family: var(--font-display);
    font-size: 0.84rem;
    font-weight: 780;
    color: var(--product-accent);
    line-height: 1.2;
  }

  .cta-arrow {
    font-size: 0.95rem;
    color: var(--product-accent);
    transition: transform var(--duration-fast) var(--ease-out-expo);
  }

  :global(.plane-card.project-proof-card--compact:hover) .cta-arrow,
  :global(.plane-card.project-proof-card--compact:focus-visible) .cta-arrow {
    transform: translateX(4px);
  }

  :global(.plane-card.project-proof-card--compact:hover) .proof-vertical-card__icon-box,
  :global(.plane-card.project-proof-card--compact:focus-visible) .proof-vertical-card__icon-box {
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 0 16px var(--product-glow);
  }

  :global(.plane-card.project-proof-card--compact:hover) .proof-vertical-card__image,
  :global(.plane-card.project-proof-card--compact:focus-visible) .proof-vertical-card__image {
    transform: scale(1.03);
    filter: saturate(1.02) contrast(1.06);
  }

  @media (max-width: 680px) {
    .proof-vertical-card__header {
      padding: 16px 16px 12px;
    }

    .proof-vertical-card__preview {
      padding: 0 14px 14px;
    }

    .proof-vertical-card__screen {
      height: 175px;
    }

    .proof-vertical-card__validation {
      padding: 0 16px 14px;
    }

    .proof-vertical-card__footer {
      padding: 12px 16px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :global(.plane-card.project-proof-card--compact),
    .proof-vertical-card__icon-box,
    .proof-vertical-card__image,
    .cta-arrow {
      transition: none;
    }

    :global(.plane-card.project-proof-card--compact:hover),
    :global(.plane-card.project-proof-card--compact:focus-visible) {
      transform: none;
    }

    :global(.plane-card.project-proof-card--compact:hover) .proof-vertical-card__icon-box,
    :global(.plane-card.project-proof-card--compact:focus-visible) .proof-vertical-card__icon-box,
    :global(.plane-card.project-proof-card--compact:hover) .proof-vertical-card__image,
    :global(.plane-card.project-proof-card--compact:focus-visible) .proof-vertical-card__image,
    :global(.plane-card.project-proof-card--compact:hover) .cta-arrow,
    :global(.plane-card.project-proof-card--compact:focus-visible) .cta-arrow {
      transform: none;
    }
  }
</style>
