---
/**
 * HarnessCodePreview.astro
 * Developer boundary Kotlin code snippet preview matching prototype (3).
 */
import { harnessData } from '../../content/harness';
const { code } = harnessData;
---

<section class="section tight" id="code">
  <div class="shell">
    <div class="code-wrap">
      <div class="reveal">
        <div class="eyebrow">{code.eyebrow}</div>
        <h2>{code.title}</h2>
        <p class="code-sub">{code.sub}</p>
      </div>
      <div class="reveal code-panel">
        <p class="code-scroll-hint" aria-hidden="true">Scroll to inspect code →</p>
        <pre tabindex="0" aria-label="LocalLlmClient example; scroll horizontally to inspect the complete code"><code><span class="kw">val</span> prepared = client.<span class="fn">prepare</span>(applicationId, useCaseId)
check(prepared.ready)

<span class="kw">val</span> sessionId = client.<span class="fn">createSession</span>(applicationId, useCaseId)
<span class="kw">val</span> handle = client.<span class="fn">generate</span>(
    request = generationRequest(sessionId, applicationId, useCaseId),
    listener = generationListener,
)

handle.<span class="fn">cancel</span>()</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .code-wrap {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
    gap: var(--space-8);
    align-items: center;
  }

  .code-wrap h2 {
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 2.8vw, 2.8rem);
    line-height: 1.12;
    color: var(--ink);
    margin: var(--space-2) 0 0;
  }

  .code-sub {
    color: var(--ink-muted);
    line-height: 1.6;
    font-size: var(--text-body);
    margin-top: var(--space-3);
  }

  .code-panel { min-width: 0; }

  .code-scroll-hint {
    display: none;
    margin: 0 0 var(--space-2);
    color: var(--ink-subtle);
    font-size: var(--text-caption);
  }

  pre {
    max-width: 100%;
    margin: 0;
    background: #0b1220;
    color: #e2e8f0;
    border-radius: var(--radius-xl);
    padding: clamp(16px, 4vw, 24px);
    overflow-x: auto;
    font-family: var(--font-mono);
    font-size: var(--text-caption);
    line-height: 1.7;
    border: 1px solid #1e293b;
    box-shadow: var(--shadow-md);
    -webkit-overflow-scrolling: touch;
  }

  code .kw { color: #93c5fd; }
  code .fn { color: #5eead4; }

  @media (max-width: 1020px) {
    .code-wrap { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .code-scroll-hint { display: block; }
  }
</style>
