name: Deploy Preview to GitHub Pages (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preview-paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain preview status
        run: |
          echo "Preview deployment is temporarily paused."
          echo "Production now deploys through the official GitHub Pages artifact workflow."
